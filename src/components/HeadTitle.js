import styled from 'styled-components'

const HeadTitle = styled.h1`
  color: #FFFFFF;
  font-size: 36px;
  line-height: ${36 / 30};
  font-weight: 300;
  margin-top: 0.5em;
  margin-bottom: 1em;
`
export default HeadTitle